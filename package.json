{
  "name": "sword-of-decay",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "validate": "esbuild scripts/validateLevel.ts --bundle --platform=node --format=esm --outfile=node_modules/.cache/validate.mjs && node node_modules/.cache/validate.mjs"
  },
  "dependencies": {
    "phaser": "^3.90.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.12",
    "typescript": "^5.7.2",
    "vite": "^6.0.5"
  }
}
